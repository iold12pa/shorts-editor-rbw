# Assets của Sếp

- `logo.png` — logo Roboworld bản TRẮNG nền trong suốt (PNG). Sếp có thể bỏ vào đây hoặc để trong folder `Claude Edit video\` — skill tự tìm cả hai nơi.
- `music/` — bỏ các file .mp3 nhạc nền được phép sử dụng vào đây; skill tự chọn bài phù hợp mood video.
- `fonts/Anton-Regular.ttf` — font Anton (Google Fonts, hỗ trợ tiếng Việt) dùng cho mọi text đè. Đã có sẵn, đừng xóa.
